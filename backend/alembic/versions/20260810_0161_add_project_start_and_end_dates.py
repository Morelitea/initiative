"""add project start and end dates

Optional whole-day schedule for a project. Stored as DATE rather than a
timestamp: a project runs for calendar days, so there is no time-of-day to
keep and no timezone to shift it by. Both columns are nullable and
independent — a project may have only a start, only an end, or neither.
"""

import sqlalchemy as sa
from alembic import op
import sqlmodel  # noqa: F401 — autogenerated types (AutoString) reference it

from app.db.guild_migrations import run_for_each_guild_schema

revision = "20260810_0161"
down_revision = "20260810_0160"
branch_labels = None
depends_on = None


def upgrade() -> None:
    run_for_each_guild_schema(op.get_bind(), _apply_upgrade)


def _apply_upgrade() -> None:
    with op.batch_alter_table("projects", schema=None) as batch_op:
        batch_op.add_column(sa.Column("start_date", sa.Date(), nullable=True))
        batch_op.add_column(sa.Column("end_date", sa.Date(), nullable=True))


def downgrade() -> None:
    run_for_each_guild_schema(op.get_bind(), _apply_downgrade)


def _apply_downgrade() -> None:
    with op.batch_alter_table("projects", schema=None) as batch_op:
        batch_op.drop_column("end_date")
        batch_op.drop_column("start_date")
