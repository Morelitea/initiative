{
  "uid": "B5RD5Y6CKW4HPC",
  "public_id": "core.event-planner",
  "kind": "dashboard",
  "name": "Event planner",
  "publisher": "Initiative",
  "description": "Everything on the calendar, read as a plan.",
  "long_description": "The initiative's events laid out the way an organizer thinks: every scheduled event on a weekly timeline, with the full list underneath for the details a bar can't carry.\n\nNothing here needs setting up — every widget reads the calendars of the initiative the dashboard lives on.",
  "avatar_url": "/marketplace/core-event-planner.svg",
  "images": [],
  "version": "1.2.0",
  "release_notes": "Counts only live calendars: one that has been archived no longer appears among the events per calendar.",
  "definition": {
    "schema_version": 1,
    "kind": "dashboard",
    "layout": {
      "columns": 12
    },
    "widgets": [
      {
        "id": "soon",
        "type": "stat",
        "grid": {
          "x": 0,
          "y": 0,
          "w": 3,
          "h": 2
        },
        "binding": {
          "source": "query",
          "sql": "SELECT count(*) AS events FROM calendar_events WHERE start_at >= now() AND start_at < now() + CAST('30 days' AS interval)"
        },
        "title": "In the next 30 days"
      },
      {
        "id": "calendars",
        "type": "chart",
        "grid": {
          "x": 3,
          "y": 0,
          "w": 9,
          "h": 4
        },
        "binding": {
          "source": "query",
          "sql": "SELECT calendar.name AS calendar, count(*) AS events FROM calendar_events WHERE calendar.archived_at IS NULL GROUP BY calendar.name ORDER BY count(*) DESC"
        },
        "title": "Events by calendar",
        "options": {
          "mark": "bar",
          "orientation": "bars"
        }
      },
      {
        "id": "schedule",
        "type": "gantt",
        "grid": {
          "x": 0,
          "y": 4,
          "w": 12,
          "h": 5
        },
        "binding": {
          "source": "query",
          "sql": "SELECT title, start_at, end_at FROM calendar_events ORDER BY start_at"
        },
        "title": "The schedule"
      },
      {
        "id": "all",
        "type": "table",
        "grid": {
          "x": 0,
          "y": 9,
          "w": 12,
          "h": 4
        },
        "binding": {
          "source": "query",
          "sql": "SELECT title, start_at, end_at, location FROM calendar_events ORDER BY start_at"
        },
        "title": "Every event"
      }
    ]
  }
}
